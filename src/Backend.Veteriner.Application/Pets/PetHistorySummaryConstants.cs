namespace Backend.Veteriner.Application.Pets;

/// <summary>Caps for pet detail history blocks (single round-trip summary). Same default count as client recent-summary.</summary>
public static class PetHistorySummaryConstants
{
    public const int RecentItemsTake = 10;
}
