namespace Backend.Veteriner.Application.Examinations;

/// <summary>Caps for examination detail related blocks (single round-trip summary). Same default as pet history-summary / client recent-summary.</summary>
public static class ExaminationRelatedSummaryConstants
{
    public const int RelatedItemsTake = 10;
}
